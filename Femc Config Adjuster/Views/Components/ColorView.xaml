<UserControl 
    x:Class="Femc_Config_Adjuster.Views.Components.ColorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="clr-namespace:Femc_Config_Adjuster.Helpers"
    xmlns:local="clr-namespace:Femc_Config_Adjuster.Views.Components"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:resx="clr-namespace:FemcConfig.Localisation.LocalisationResources;assembly=FemcConfig.Localisation"
    d:DesignHeight="64" 
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:BrushToConfigColor x:Key="BrushToConfigColor" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Border
            Margin="8"
            Padding="8"
            Background="{DynamicResource SubtleFillColorSecondaryBrush}"
            CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="1*" MaxWidth="96" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="{Binding Name}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap"
                    ToolTip="{Binding Name}" />
                <UniformGrid 
                    Grid.Column="1"
                    Background="{Binding Color, Converter={StaticResource BrushToConfigColor}}"
                    Height="36"/>
                <TextBlock
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    FontWeight="SemiBold"
                    FontSize="14"
                    Text="{Binding ColorHex}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap"
                    ToolTip="{Binding ColorHex}"
                />
                <Button 
                    Grid.Column="3"
                    Margin="8,0,0,0"
                    Click="ButtonBase_OnClick"
                    Content="Edit"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
